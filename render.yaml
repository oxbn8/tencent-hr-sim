services:
  - type: web
    name: tencent-hr-sim
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: LLM_PROVIDER
        value: mock
      - key: IMAGE_PROVIDER
        value: mock
      - key: STATIC_DIR
        value: /app/static
